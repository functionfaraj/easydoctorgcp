steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/easydoctor-api:$BUILD_ID', '.']
    timeout: '600s'

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/easydoctor-api:$BUILD_ID']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'easydoctorgcp'
      - '--image'
      - 'gcr.io/$PROJECT_ID/easydoctor-api:$BUILD_ID'
      - '--platform'
      - 'managed'
      - '--region'
      - 'us-central1'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--set-env-vars'
      - 'NODE_ENV=production,MONGODB_URL=mongodb+srv://functionfaraj:FMcCiwMQ1nPbKnTs@cluster0.xdnqskl.mongodb.net/EasyDoctorDB,JWT_SECRET=SECRET#123,JWT_EXP=50m'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--timeout'
      - '900s'
      - '--cpu-throttling'
      - '--concurrency'
      - '80'

# Substitution variables that can be set in Cloud Build triggers
substitutions:
  _JWT_SECRET: 'SECRET#123'
  _MONGODB_URL: 'mongodb+srv://functionfaraj:FMcCiwMQ1nPbKnTs@cluster0.xdnqskl.mongodb.net/EasyDoctorDB'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'

timeout: '1200s'